git --version
git config --global user.name "Hadi-
git config --global user.email "hadmak10@gmail.com"
git init
git status
git add index.html
git add --all (git add -A)
Short status (git status --short) flags are:

?? - Untracked files
A - Files added to stage
M - Modified files
D - Deleted files

git commit -a -m "Updated index.html with a new line"
git commit -m "First release of Hello World!"
git log
git command -help -  See all the available options for the specific command
git help --all -  See all possible commands
git command --help Opens the manual 
git help --all SHIFT + G to jump the end of the list, then q to exit the view
git branch hello-world-images
git branch
git checkout hello-world-images